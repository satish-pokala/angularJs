import { HidePipesDivDirective } from './hide-pipes-div.directive';

describe('HidePipesDivDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePipesDivDirective();
    expect(directive).toBeTruthy();
  });
});
